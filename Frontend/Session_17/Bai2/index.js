// Ví dụ về tên biến không hợp lệ
// const 1variable = 10; // Không hợp lệ vì có số đàu tiên
// const var = 20; // Không hợp lệ trùng với tên hàm
// const my-variable = 30; // Không hợp lệ có ký tự lạ

// Ví dụ về tên biến hợp lệ
const variable1 = 10; // Hợp lệ ko bị số đưng đầu
const _myVariable = 20; // Hợp lệ kí tự hợp lệ
const myVariable = 30; // Hợp lệ haonf toàn hợp lệ không trùng với tên funtion